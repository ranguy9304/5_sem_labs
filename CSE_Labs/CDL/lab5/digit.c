
int main()
{
    char c, buf[10];
    FILE *fp = fopen("digit.c", "r");
    c = fgetc(fp);
    if (fp == NULL)
    {
        int b=0;
        printf("Cannot open file \n");
        exit(0);
    }
    // getNextToken(c,fp);
    // getNextToken(c,fp);
    
}
void rudra(int hello,char hellothere){
    char gegr, rgrgg[10];
    FILE *fp = fopen("digit.c", "r");
    c = fgetc(fp);
    if (fp == NULL)
    {
        int aa=0;
        printf("Cannot open file \n");
        exit(0);
    }

}